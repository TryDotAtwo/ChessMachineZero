# Fresh protocol evidence

- RED: `py -m pytest tests/test_protocol.py -q -W error` failed because `vm_compiler` did not exist.
- GREEN: the same command passed `15` exact tests.
- `git diff --check` passed.
- Branch started as an empty orphan and contains no migrated runtime source.
